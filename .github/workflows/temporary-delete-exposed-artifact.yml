name: Temporary delete exposed artifact
on:
  workflow_dispatch:
permissions:
  actions: write
jobs:
  delete:
    runs-on: ubuntu-latest
    steps:
      - name: Delete artifact
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh api -X DELETE /repos/breverdbidder/jev-seo/actions/artifacts/10725348336
